import 'package:flutter/material.dart';

class UiSearchPage extends StatelessWidget {
  const UiSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Search Page"),
      ),
    );
  }
}
